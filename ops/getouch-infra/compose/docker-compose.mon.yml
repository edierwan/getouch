# Monitoring & observability stack
# Usage: docker compose -f docker-compose.mon.yml up -d

services:
  prometheus:
    image: prom/prometheus:latest
    container_name: prometheus
    restart: unless-stopped
    environment:
      TZ: Asia/Kuala_Lumpur
    volumes:
      - /opt/getouch/monitoring/prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - /opt/getouch/monitoring/alert-rules.yml:/etc/prometheus/alert-rules.yml:ro
      - /data/prometheus:/prometheus
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
      - '--storage.tsdb.retention.time=30d'
    networks:
      - getouch_data
      - getouch_app
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:9090/-/healthy"]
      interval: 30s
      timeout: 5s
      retries: 3
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

  grafana:
    image: grafana/grafana:latest
    container_name: grafana
    restart: unless-stopped
    environment:
      GF_SECURITY_ADMIN_USER: ${GF_SECURITY_ADMIN_USER:-admin}
      GF_SECURITY_ADMIN_PASSWORD: ${GF_SECURITY_ADMIN_PASSWORD:-admin}
      # ── Domain ──
      GF_SERVER_DOMAIN: grafana.getouch.co
      GF_SERVER_ROOT_URL: https://grafana.getouch.co
      # ── Auth proxy (Cloudflare Access SSO) ──
      # Caddy rewrites the CF header to X-WEBAUTH-USER for Grafana
      GF_AUTH_PROXY_ENABLED: "true"
      GF_AUTH_PROXY_HEADER_NAME: X-WEBAUTH-USER
      GF_AUTH_PROXY_HEADER_PROPERTY: email
      GF_AUTH_PROXY_AUTO_SIGN_UP: "true"
      GF_AUTH_PROXY_HEADERS: "Email:X-WEBAUTH-USER"
      GF_AUTH_PROXY_WHITELIST: ""
      GF_AUTH_PROXY_SYNC_TTL: "60"
      GF_AUTH_PROXY_ENABLE_LOGIN_TOKEN: "true"
      # ── Auto-assign users ──
      GF_USERS_AUTO_ASSIGN_ORG: "true"
      GF_USERS_AUTO_ASSIGN_ORG_ROLE: Admin
      GF_USERS_ALLOW_SIGN_UP: "false"
      # ── Disable unnecessary auth ──
      GF_AUTH_BASIC_ENABLED: "false"
      GF_AUTH_DISABLE_LOGIN_FORM: "true"
      GF_AUTH_DISABLE_SIGNOUT_MENU: "false"
      GF_AUTH_ANONYMOUS_ENABLED: "false"
      # ── Security ──
      GF_SECURITY_COOKIE_SECURE: "true"
      GF_SECURITY_ALLOW_EMBEDDING: "false"
      TZ: Asia/Kuala_Lumpur
    volumes:
      - /data/grafana:/var/lib/grafana
      - /opt/getouch/monitoring/grafana/provisioning:/etc/grafana/provisioning:ro
      - /opt/getouch/monitoring/grafana/dashboards:/var/lib/grafana/dashboards:ro
    networks:
      - getouch_data
      - getouch_app
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:3000/api/health"]
      interval: 30s
      timeout: 5s
      retries: 3
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

  node-exporter:
    image: prom/node-exporter:latest
    container_name: node-exporter
    restart: unless-stopped
    pid: host
    environment:
      TZ: Asia/Kuala_Lumpur
    volumes:
      - /proc:/host/proc:ro
      - /sys:/host/sys:ro
      - /:/rootfs:ro
    command:
      - '--path.procfs=/host/proc'
      - '--path.sysfs=/host/sys'
      - '--path.rootfs=/rootfs'
    networks:
      - getouch_data
    logging:
      driver: json-file
      options:
        max-size: "5m"
        max-file: "3"

  cadvisor:
    image: gcr.io/cadvisor/cadvisor:latest
    container_name: cadvisor
    restart: unless-stopped
    environment:
      TZ: Asia/Kuala_Lumpur
    volumes:
      - /:/rootfs:ro
      - /var/run:/var/run:ro
      - /sys:/sys:ro
      - /var/lib/docker/:/var/lib/docker:ro
    networks:
      - getouch_data
    logging:
      driver: json-file
      options:
        max-size: "5m"
        max-file: "3"

networks:
  getouch_data:
    external: true
  getouch_app:
    external: true
